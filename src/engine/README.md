# src/engine

Timing math: cycle projection, force-offs, offset references, clearance calculators, and later
bandwidth and offset optimization.

Pure TypeScript. No React, MUI, Tauri, `src/ui`, `src/io` or `src/manager` imports; the
`test/boundaries.test.ts` suite enforces it.
