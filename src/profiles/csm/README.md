# src/profiles/csm

The CSM ASC-3 controller profile: checks a SPATT intersection against what the City Super Mod's
ASC-3 controller can express, and converts plans to and from its timing-plan JSON (ticks,
CSM's own offset reference).

Pure TypeScript, same boundary rules as `src/engine`.
